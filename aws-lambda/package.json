{
  "name": "aws-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prep": "npm i",
    "build": "tsc --listFiles --listEmittedFiles --diagnostics --pretty",
    "watch": "tsc -w",
    "cdk:deploy": "cdk deploy --require-approval never",
    "cdk:diff": "cdk diff",
    "cdk:synth": "cdk synth"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "aws-cdk-lib": "^2.87.0",
    "constructs": "^10.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "aws-sdk": "^2.1370.0"
  }
}
